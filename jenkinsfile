pipeline {
    agent {
        dockerfile {
            args '-v /home/bruno:/home/bruno'
        }
    }
    stages {
        stage('build') {
            steps {
                sh 'whoami'
                sh 'python src/main.py'
            }
        }
    }
}