pipeline {
    agent any
    stages {
        stage('docker-compose') {
            steps {
                sh "docker-compose build"
                sh "docker-compose up -d"
                sh 'docker logs -t --until=10m $(docker-compose ps -q cerberus)'
            }
        }
    }
}