pipeline {
    agent {
        docker {
            image 'python:3.9-alpine'
            args '-v $HOME:$HOME'
        }
    }
    stages {
        stage('build') {
            steps {
                sh 'python src/main.py'
            }
        }
    }
}