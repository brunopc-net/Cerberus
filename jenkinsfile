pipeline {
    agent {
        dockerfile {
            args '-v /home/bruno:/home/bruno'
        }
    }
    stages {
        stage('build') {
            steps {
                sh 'whoami'
                sh 'ls -lrt'
                sh 'ls /home/bruno'
                sh 'python src/main.py'
            }
        }
    }
}