pipeline {
    agent any
    stages {
        stage('docker-compose') {
            steps {
                sh "docker-compose build"
                sh "docker-compose up -d"
                sh 'docker logs -t $(docker-compose ps -q cerberus)'
                sh 'docker stop cerberus'
                sh 'docker rm cerberus'
            }
        }
    }
}