pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh "docker compose build"
                sh "docker compose create"
                sh "docker compose run"
            }
        }
    }
    post {
        always {
            sh 'docker system prune -a -f'
            echo 'Bye bye!!!'
        }
    }
}