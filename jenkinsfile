pipeline {
    agent {
        dockerfile true
    }
    stages {
        stage('build') {
            steps {
                sh 'echo "$PWD"'
                sh 'ls'
                sh 'python app/src/main.py'
            }
        }
    }
}